name = "brew-day-water-helper"
compatibility_date = "2024-12-01"
main = "src/worker.ts"

# Serve the built React SPA from assets
assets = { directory = "./dist" }

[[d1_databases]]
binding = "DB"
database_name = "brew-day-db"
database_id = "brew-day-db"
